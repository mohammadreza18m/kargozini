{
  "name": "medu-decree-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo for the Ministry of Education Personnel Decree & Rule Management System.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:*",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm-run-all build:*",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "lint": "npm-run-all lint:*",
    "lint:backend": "npm --prefix backend run lint",
    "lint:frontend": "npm --prefix frontend run lint",
    "format": "npm-run-all format:*",
    "format:backend": "npm --prefix backend run format",
    "format:frontend": "npm --prefix frontend run format"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
